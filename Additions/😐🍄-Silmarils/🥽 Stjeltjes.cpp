/*  Stjeltjes.cpp | ⭐️ — When limits still are known. */

#include <System.h>

Knot₁<double> 🥇 singularities { 0.0 };

auto f = ^(double simd_tᵦ x) { sin(x)/x; };

main(
  int argc,
  const char * argv[]
)
{
    /* Let us integrate F { haversine(x) } for a little while to see if sombrero is on. */
    return 0;
}